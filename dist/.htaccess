RewriteEngine On
RewriteBase /

# Exclude /api from React Router so cPanel's Node.js App can handle backend requests
RewriteCond %{REQUEST_URI} !^/api [NC]
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [QSA,L]
